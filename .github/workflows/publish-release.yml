name: Publish Release

on:
  workflow_call:
    secrets:
      NPM_TOKEN:
        required: true
      SLACK_WEBHOOK_URL:
        required: true
      PUBLISH_DOCS_TOKEN:
        required: true
      INFURA_API_KEY:
        required: false

jobs:
  publish-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout and setup environment
        uses: MetaMask/action-checkout-and-setup@v1
        with:
          is-high-risk-environment: true
      - uses: MetaMask/action-publish-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Restore Turbo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-release-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-release-
      - run: yarn build
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: publish-release-artifacts-${{ github.sha }}
          include-hidden-files: true
          retention-days: 4
          path: |
            ./packages/**/dist
            ./playground/**/dist
            ./node_modules/.yarn-state.yml

  publish-npm-dry-run:
    name: Dry run publish to NPM
    runs-on: ubuntu-latest
    needs: publish-release
    steps:
      - name: Checkout and setup environment
        uses: MetaMask/action-checkout-and-setup@v1
        with:
          is-high-risk-environment: true
          ref: ${{ github.sha }}
      - name: Restore build artifacts
        uses: actions/download-artifact@v4
        with:
          name: publish-release-artifacts-${{ github.sha }}
      - name: Dry run publish to NPM
        uses: MetaMask/action-npm-publish@v5
        with:
          slack-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          subteam: S042S7RE4AE # @metamask-npm-publishers

  publish-npm:
    name: Publish to NPM
    environment: npm-publish
    runs-on: ubuntu-latest
    needs: publish-npm-dry-run
    steps:
      - name: Checkout and setup environment
        uses: MetaMask/action-checkout-and-setup@v1
        with:
          is-high-risk-environment: true
          ref: ${{ github.sha }}
      - name: Restore build artifacts
        uses: actions/download-artifact@v4
        with:
          name: publish-release-artifacts-${{ github.sha }}
      - name: Publish to NPM
        uses: MetaMask/action-npm-publish@v5
        with:
          npm-token: ${{ secrets.NPM_TOKEN }}

  get-release-version:
    needs: publish-npm
    runs-on: ubuntu-latest
    outputs:
      RELEASE_VERSION: ${{ steps.get-release-version.outputs.RELEASE_VERSION }}
      RELEASE_TAG: ${{ steps.get-release-version.outputs.RELEASE_TAG }}
      RN_PLAYGROUND_RELEASED: ${{ steps.check-rn-playground.outputs.RELEASED }}
      RN_PLAYGROUND_TAG: ${{ steps.check-rn-playground.outputs.PLAYGROUND_TAG }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - id: get-release-version
        shell: bash
        run: |
          # Get version from the latest git tag created by the release action
          TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$TAG" ]; then
            # Fallback to package.json version if no tag found
            TAG="v$(node -p "require('./package.json').version")"
          fi
          echo "RELEASE_TAG=$TAG" >> "$GITHUB_OUTPUT"
          # Remove 'v' prefix for the version output
          VERSION=${TAG#v}
          echo "RELEASE_VERSION=$VERSION" >> "$GITHUB_OUTPUT"
      - id: check-rn-playground
        shell: bash
        run: |
          PKG_NAME=$(node -p "require('./playground/react-native-playground/package.json').name")
          PKG_VERSION=$(node -p "require('./playground/react-native-playground/package.json').version")
          PLAYGROUND_TAG="${PKG_NAME}@${PKG_VERSION}"
          echo "PLAYGROUND_TAG=$PLAYGROUND_TAG" >> "$GITHUB_OUTPUT"

          # Check that the tag exists AND was created on this commit.
          # A tag from a prior release would point to an older commit.
          TAG_COMMIT=$(git rev-list -n 1 "refs/tags/$PLAYGROUND_TAG" 2>/dev/null || echo "")

          if [ "$TAG_COMMIT" = "${{ github.sha }}" ]; then
            echo "RELEASED=true" >> "$GITHUB_OUTPUT"
            echo "$PLAYGROUND_TAG points to ${{ github.sha }} — released in this run"
          else
            echo "RELEASED=false" >> "$GITHUB_OUTPUT"
            if [ -z "$TAG_COMMIT" ]; then
              echo "$PLAYGROUND_TAG does not exist — playground was not released"
            else
              echo "$PLAYGROUND_TAG points to $TAG_COMMIT, not ${{ github.sha }} — stale tag from a prior release"
            fi
          fi

  publish-wagmi-to-gh-pages:
    name: Publish Wagmi Integration to `${{ needs.get-release-version.outputs.RELEASE_VERSION }}` directory of `gh-pages` branch
    needs: get-release-version
    permissions:
      contents: write
    uses: ./.github/workflows/deploy-wagmi-to-gh-pages.yml
    with:
      destination_dir: ${{ needs.get-release-version.outputs.RELEASE_VERSION }}
    secrets:
      PUBLISH_DOCS_TOKEN: ${{ secrets.PUBLISH_DOCS_TOKEN }}

  publish-wagmi-to-latest-gh-pages:
    name: Publish Wagmi Integration to `latest` directory of `gh-pages` branch
    needs: publish-wagmi-to-gh-pages
    permissions:
      contents: write
    uses: ./.github/workflows/deploy-wagmi-to-gh-pages.yml
    with:
      destination_dir: latest
    secrets:
      PUBLISH_DOCS_TOKEN: ${{ secrets.PUBLISH_DOCS_TOKEN }}

  publish-browser-playground-to-gh-pages:
    name: Publish Browser Playground to `${{ needs.get-release-version.outputs.RELEASE_VERSION }}` directory of `gh-pages` branch
    needs: get-release-version
    permissions:
      contents: write
    uses: ./.github/workflows/deploy-browser-playground-to-gh-pages.yml
    with:
      destination_dir: ${{ needs.get-release-version.outputs.RELEASE_VERSION }}
    secrets:
      PUBLISH_DOCS_TOKEN: ${{ secrets.PUBLISH_DOCS_TOKEN }}

  publish-browser-playground-to-latest-gh-pages:
    name: Publish Browser Playground to `latest` directory of `gh-pages` branch
    needs: publish-browser-playground-to-gh-pages
    permissions:
      contents: write
    uses: ./.github/workflows/deploy-browser-playground-to-gh-pages.yml
    with:
      destination_dir: latest
    secrets:
      PUBLISH_DOCS_TOKEN: ${{ secrets.PUBLISH_DOCS_TOKEN }}

  build-rn-playground-apk:
    name: Build and publish RN Playground APK
    needs: get-release-version
    if: needs.get-release-version.outputs.RN_PLAYGROUND_RELEASED == 'true'
    permissions:
      contents: write
    uses: ./.github/workflows/build-rn-playground-apk.yml
    with:
      release_tag: ${{ needs.get-release-version.outputs.RN_PLAYGROUND_TAG }}
      upload_to_release: true
    secrets:
      INFURA_API_KEY: ${{ secrets.INFURA_API_KEY }}
